# Start

`cp .env.example .env`
for docker .env database is configured

### for Docker:

run   `make init`

url: localhost:58080

run seeders `make migrate-seed`

run test `make test`

down : `make down`
